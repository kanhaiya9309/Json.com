<!DOCTYPE html>
<html>

<head>
  <title>Graduation</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Graduation Complete 🎓</h1>

  <form action="/graduation" method="POST" class="buttons">
    <button type="submit" value="kanhaiya" name="choice">👩🏽‍🏫</button>
    <button type="submit" value="kunal" name="choice">👩🏽‍🏫</button>
    <button type="submit" value="tushar" name="choice">🧑‍🎓</button>
  </form>

  <div id="recipeContainer" class="hidden">
 <% if(locals.graduation){  %>
    <h2 id="recipeTitle">
        <%= graduation.name %>
    </h2>
      <h3>Education:</h3>
      <ul id="ingredientsList">
        <li>
          <%= graduation.education.college.name %>, <%= graduation.education.college.preparation %>
        </li>
        <h3>Skill and Project:</h3>
        <li>
          <%= graduation.education.Skill.name %>
        </li>
        <% graduation.education.Language.forEach(Language=> { %>
          <li>
            <%= Language.projects %> of <%= Language.name %>
          </li>
          <% }) %>
      </ul>
      <%} else {%>
        <h2>Pick your favourite Student 👆</h2>
        <%}%>
  </div>
  
</body>

</html>

